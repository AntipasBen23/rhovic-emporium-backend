GitHub Actions secrets/variables for this repo:

- `RAILWAY_DEPLOY_HOOK_URL` (secret, optional): Railway deploy webhook for auto-deploy on `main`

No extra environment variables are required for CI test runs.
